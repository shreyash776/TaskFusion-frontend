// const HOST = process.env.REACT_APP_HOST_URL;
const HOST = "http://localhost:8080/";

export const Routes = {
  AUTH: HOST + "auth",
  BOARD: HOST + "board",
  LIST: HOST + "list",
  CARD: HOST + "card",
};
